// import { SplitData, Transaction } from "../types";

// export const updateExpenses = (expenses: Record<string, number>, transaction: Transaction, add = true) => {
//   const negative = add ? 1 : -1;
//   const nextExpenses = {...expenses};
//   const splitData = transaction.splitData;

//   if (splitData.type === 'balanced'){
//     for(let memberUid of splitData.data.payingMembers){
//       nextExpenses[memberUid] = (nextExpenses[memberUid] ?? 0) + ()
//     }
//   }
// }
